apiVersion: 1

datasources:
  - name: Postgres
    type: postgres
    access: proxy
    url: postgres:5432
    database: myapp_db
    user: myapp_user
    secureJsonData:
      password: myapppass
    jsonData:
      sslmode: "disable"
    isDefault: true